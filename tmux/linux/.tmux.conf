# ==============================================================================
# TMUX CONFIGURATION
# ==============================================================================
# Plugin Manager: git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

# ==============================================================================
# GENERAL
# ==============================================================================
set-option -g default-shell /bin/zsh
set -s escape-time 0
set -g history-limit 50000
set -g base-index 1
set -g pane-base-index 1
set-option -g renumber-windows on
setw -g xterm-keys on
setw -g automatic-rename

# ==============================================================================
# PREFIX KEY (Ctrl+A)
# ==============================================================================
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# ==============================================================================
# MOUSE
# ==============================================================================
set -g mouse on
bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'copy-mode -e; send-keys -M'"
unbind -n MouseDown3Pane
set -ga terminal-overrides 'xterm*:smcup@:rmcup@'
bind m set-option -g mouse \; display "Mouse: #{?mouse,ON,OFF}"
bind -n F8 set-option -g mouse \; display "Mouse: #{?mouse,ON,OFF}"

# ==============================================================================
# WINDOWS AND SESSION
# ==============================================================================
bind N new-window
bind -n M-n new-window
bind -n M-Tab next-window
bind -n M-a previous-window
bind -n M-pagedown next-window
bind -n M-pageup previous-window
bind -n M-s next-window
bind -n M-i command-prompt -I "#W" "rename-window '%%'"
bind -n M-I command-prompt -I "#S" "rename-session '%%'"
bind i command-prompt -I "#W" "rename-window '%%'"
bind -n M-w choose-tree -Zw
bind -n M-= choose-tree -Zs
bind-key -n M-d detach-client
bind-key q kill-pane
bind-key x kill-session

# start window numbering at 1
set -g base-index 1
# start pane numbering at 1 as well
set -g pane-base-index 1
# renumber windows when one is closed
set -g renumber-windows on

# ==============================================================================
# PANES – NAVIGATION
# ==============================================================================
bind -n M-Down select-pane -D
bind -n M-J select-pane -D
bind -n M-Left select-pane -L
bind -n M-H select-pane -L
bind -n M-Right select-pane -R
bind -n M-L select-pane -R
bind -n M-Up select-pane -U
bind -n M-K select-pane -U
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# ==============================================================================
# PANES – SPLIT / RESIZE / SWAP / KILL
# ==============================================================================
bind -n M-8 split-window -v
bind -n M-9 split-window -h
bind -n M-S-Up resize-pane -U 5
bind -n M-S-Down resize-pane -D 5
bind -n M-S-Right resize-pane -R 5
bind -n M-S-Left resize-pane -L 5
bind -n M-z resize-pane -Z
bind -n M-f resize-pane -Z
bind -n M-q kill-pane
bind -n M-x kill-session
bind -n S-F5 setw synchronize-panes
bind H splitw -fhb \; swapp -t ! \; killp -t !
bind L splitw -fh  \; swapp -t ! \; killp -t !
bind J splitw -fv  \; swapp -t ! \; killp -t !
bind K splitw -fvb \; swapp -t ! \; killp -t !
bind j swap-pane -D
bind k swap-pane -U
bind h swap-pane -s '{left-of}'
bind l swap-pane -s '{right-of}'

# ==============================================================================
# COPY / YANK
# ==============================================================================
bind-key -T copy-mode y send-keys -X copy-pipe-and-cancel "xsel -i -p && xsel -o -p | xsel -i -b"
bind-key C-y run "xsel -o | tmux load-buffer - ; tmux paste-buffer"

# ==============================================================================
# TERMINAL
# ==============================================================================
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",xterm-256color:Tc"
set -ga terminal-overrides ",*:kh=\E[H:@7=\E[F"
set -g terminal-overrides ',*:Ss=\E[%p1%d q:Se=\E[1 q'

# ==============================================================================
# HOME / END FIX
# ==============================================================================
bind-key -n Home send Escape "[H"
bind-key -n End send Escape "[F"

# ==============================================================================
# CURSOR
# ==============================================================================
set -g cursor-style blinking-bar

# ==============================================================================
# STATUS BAR
# ==============================================================================
set -g status-left ''
set -g status-left-length 0
set -g status-right '%d.%m.%Y|%H:%M'
set -g status-right-length 20

# ==============================================================================
# CONFIG RELOAD
# ==============================================================================
bind r source-file ~/.tmux.conf \; display-message "~/.tmux.conf reloaded mate"

# ==============================================================================
# PLUGINS
# ==============================================================================
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @resurrect-save 'S'
set -g @resurrect-restore 'R'
# todo
set -g @continuum-restore 'off'
set -g @resurrect-processes 'btop nload journalctl yazi ssh auto-cpufreq'

# ==============================================================================
# THEME
# ==============================================================================
# source-file ~/opt/git/dotfiles_linux/tmux/linux/tmux.theme.conf

# ==============================================================================
# INITIALIZE TPM (must be after plugins and theme)
# ==============================================================================
run '~/.tmux/plugins/tpm/tpm'

# ==============================================================================
# PERSONAL OVERRIDES (after TPM to override theme defaults)
# ==============================================================================
set -g window-active-style 'bg=#000000'
set -g window-style 'bg=#1a1a1a'        # noch dunkler für inaktiv
set -g pane-border-style 'fg=#3c3836'   # dunkler grau
set -g pane-active-border-style 'fg=#ff5555,bold'
# # set -g pane-active-border-style 'fg=#a6e3a1'
# # set -g pane-active-border-style 'fg=#d79921'

# Optional: aktives Pane mit farbigem Hintergrund in der Statuszeile
set -g pane-border-status top
set -g pane-border-format "#{?pane_active,#[fg=#ff5555]▶ #P,#[fg=#504945]  #P}"


# ==============================================================================
# TESTING
# ==============================================================================

